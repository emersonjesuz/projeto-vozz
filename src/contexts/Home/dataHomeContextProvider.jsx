/* import userHomeProvider from '../../hooks/userHomeProvider';
import DataHomeContext from './UserHomeContext';

function DataHomeContextProvider(props) {
    const userProvider = userHomeProvider();

    return (
        <DataHomeContext.Provider value={userProvider} >
            {props.children}
        </DataHomeContext.Provider>
    );
}

export default DataHomeContextProvider; */
